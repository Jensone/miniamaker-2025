<div class="d-flex align-items-center gap-3">
    {% if app.user %}
        <a href="{{ path('app_logout') }}" class="text-decoration-none text-dark">
            Déconnexion
        </a>
    {% else %}
        <a href="{{ path('app_login') }}" class="text-decoration-none text-dark">
            Connexion
        </a>
        <a href="{{ path('app_register') }}" class="text-decoration-none text-dark">
            Inscription
        </a>
    {% endif %}
</div>